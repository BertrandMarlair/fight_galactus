const style = (theme) => {
    return {
        root: {
            height: "100%",
            display: "flex",
            backgroundColor: theme.palette.background.default,
        },
    };
};

export default style;
