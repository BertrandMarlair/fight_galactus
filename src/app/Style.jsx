const style = (theme) => {
    return {
        root: {
            minHeight: "100%",
            display: "flex",
            backgroundColor: theme.palette.background.default,
        },
    };
};

export default style;
