export const publicDefaultRoute = "/connect/login";

const publicRoutes = [];

export const defaultRoute = "/";
export const name = "Public";
export const slug = "public";

export default publicRoutes;
